def f(x, y):
    if x > y or x == 17:
        return 0
    elif x == y:
        return 1
    return f(x + 1, y) + f(x * 2, y)


print(f(1, 10) * f(10, 35))
# не содержит 17
# содержит 10
# начало 1
# результат 35


s = set()
def f(a, k):
    if k > 13:
        return 0
    if k == 13:
        if a < 0:
            s.add(a)
    if k < 13:
        f(a - 3, k + 1)
        f(-3 * a, k+ 1)
f(333, 0)
print(len(s))
