from turtle import *

left(90)
for i in range(16):
    left(36)
    forward(4 * 50)
    left(36)
    # цикл движений черепахи
penup()
# черепаха не рисует
for x in range(10):
    for y in range(10):
        goto(x * 50, y * 50)
        dot(5)
        # создание точек по ДСК размером 5 пикс
        # если поле не охватывает фигуру полностью нужно изменять знаки х, у в goto()
done()
# методы Turtle:
# forward() / backward() - движение черепахи
# back() / right() / left() - изменение напрвления
# pendown() / penup() - поднять / опустить хвост
